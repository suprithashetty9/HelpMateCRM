<%= form_with(model: caller_type, local: true) do |form| %>
  <% if caller_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caller_type.errors.count, "error") %> prohibited this caller_type from being saved:</h2>

      <ul>
      <% caller_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :callertype %>
    <%= form.text_field :callertype %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
