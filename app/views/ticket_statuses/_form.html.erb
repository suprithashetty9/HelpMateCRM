<%= form_with(model: ticket_status, local: true) do |form| %>
  <% if ticket_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_status.errors.count, "error") %> prohibited this ticket_status from being saved:</h2>

      <ul>
      <% ticket_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <meta name="viewport" content="width=device-width, initial-scale=1">

<div class="panel panel-default">
  <div class="panel-body">
   <div class="form-group margin-vertical-none">
          

      <%= form.label :status, :class=>"form-check-label"  %>
  <%= form.text_field :status, :class  =>"form-control input-sm" %>
    </div>
</div>
</div>
 <div class="form-group margin-vertical-none">
         &nbsp;&nbsp;
          <%= form.submit "Submit", class: "btn btn-primary" %>
          <%= link_to 'Back', ticket_statuses_path, :class=>'btn btn-primary' %>
          <% end %>
         </div>
         </div>
    
  </div>
</div>
