<%= form_with(model: ticket_attachment, local: true) do |form| %>
  <% if ticket_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_attachment.errors.count, "error") %> prohibited this ticket_attachment from being saved:</h2>

      <ul>
      <% ticket_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ticketattachment %>
    <%= form.text_field :ticketattachment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
