<%= form_with(model: age, local: true) do |form| %>
  <% if age.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age.errors.count, "error") %> prohibited this age from being saved:</h2>

      <ul>
      <% age.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
