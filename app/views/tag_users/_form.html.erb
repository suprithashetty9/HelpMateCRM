<%= form_with(model: tag_user, local: true) do |form| %>
  <% if tag_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_user.errors.count, "error") %> prohibited this tag_user from being saved:</h2>

      <ul>
      <% tag_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tagusers %>
    <%= form.text_field :tagusers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
