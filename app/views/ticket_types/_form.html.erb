<%= form_with(model: ticket_type, local: true) do |form| %>
  <% if ticket_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_type.errors.count, "error") %> prohibited this ticket_type from being saved:</h2>

      <ul>
      <% ticket_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tickettype %>
    <%= form.text_field :tickettype %>
  </div>

  <div class="field">
    <%= form.label :createdby %>
    <%= form.number_field :createdby %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
