<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employeename %>
    <%= form.text_field :employeename %>
  </div>

  <div class="field">
    <%= form.label :employeecode %>
    <%= form.number_field :employeecode %>
  </div>

  <div class="field">
    <%= form.label :employeedesg %>
    <%= form.text_field :employeedesg %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
