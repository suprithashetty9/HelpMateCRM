<%= form_with(model: product_sku, local: true) do |form| %>
  <% if product_sku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_sku.errors.count, "error") %> prohibited this product_sku from being saved:</h2>

      <ul>
      <% product_sku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :productsku %>
    <%= form.text_field :productsku %>
  </div>

  <div class="field">
    <%= form.label :createdby %>
    <%= form.number_field :createdby %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
